#include <iostream>

int main() {
    // 1. Объявить i типа int
    int i;
    // 2. Инициализировать на 69
    i = 69;
    // 3. Определить переменную number типа int, дать значение 72
    int number = 72;
    // 4. Перезаписать значение i на 69 + 72
    i = i + number;
    std::cout<<i<<"\n";
    // 5. Определить группу, массив переменных из 3 элементов (инициализированных на 0)
    int array[3] = {0, 0, 0};
    // 6. Создать указатель на i (iPointer)
    int* iPointer = &i;
    // 7. Считать данные по адресу из указателя в переменную number
    number = *iPointer;
    std::cout<<number<<"\n";
    // 8. Вписать в i через указатель значение 15
    *iPointer = 15;
    std::cout<<i<<"\n";
    // 9. Сменить адрес в iPointer на адрес первого элемента массива
    iPointer = &array[0];
    // 10. Переместить адресс в указателе iPointer на третий элемент массива
    iPointer = iPointer + 2;
    // 11. Вписать в первый элемент массива напрямую, и в третий через iPointer, значения 5 и 6
    array[0] = 5;
    *iPointer = 6;
    for(int j=0; j<3;j++){
std::cout<<array[j]<<" ";}
    return 0;
}
